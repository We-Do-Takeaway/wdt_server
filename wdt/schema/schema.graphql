type Section {
  id: ID!
  description: String
  name: String!
  photo: String
  displayOrder: Int
}

type Menu {
  id: ID!
  description: String
  name: String!
  photo: String
  sections: [Section]
}

type Query {
    menus: [Menu!]!
    menu(id: ID!): Menu
}
